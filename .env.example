# Copy this file to .env and adjust values as needed.

# --- Logging ---
LOGLEVEL=INFO

# --- LLM Provider API keys (optional; set only those you use) ---
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
GOOGLE_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# --- LLM Model config ---
LLM_MODEL=openai/gpt-4o
LLM_BASE_URL=

# --- Neo4j connection ---
# DSN format: scheme://[username[:password]@]host[:port][/database]
# Allowed schemes: neo4j, neo4j+s, neo4j+ssc, bolt, bolt+s, bolt+ssc
NEO4J_DSN=bolt://neo4j:secret@localhost:7687/neo4j

# --- Tests ---
# Enable integration tests (requires Docker running)
# RUN_NEO4J_IT=1
# Optional: password the testcontainer uses (if your setup expects it)
# NEO4J_PASSWORD=secret
